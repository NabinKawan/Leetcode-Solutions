class Solution:
    def fib(self, n: int) -> int:
        numx = n-1
        numy = n-2
        numz = 0
        numo = 0
        for i in range(len(numx)):
          numz += i
        for i in range(len(numo)):
          numz += i
        return numz+numy